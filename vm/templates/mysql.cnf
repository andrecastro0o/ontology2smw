[server]
user=root
port=3306
lower_case_table_names=1
max_allowed_packet=16777216

[client]
user=root
password="{{ mariadb_root_pass }}"

[mysql]
user=root
password="{{ mariadb_root_pass }}"

[mysqldump]
user=root
password="{{ mariadb_root_pass }}"

[mysqldiff]
user=root
password="{{ mariadb_root_pass }}"